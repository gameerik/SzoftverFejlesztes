package Model.GameComponents;

/**
 * Enum class representing the possible states of a {@link Cell}.
 */
public enum State {

    /**
     * Represents an empty cell.
     */
    EMPTY,

    /**
     * Represents a filled cell, which is part of a Domino.
     */
    DOMINO
}
